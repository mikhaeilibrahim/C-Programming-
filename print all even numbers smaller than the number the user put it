#include <stdio.h>
#include <stdlib.h>

int main()
{

    unsigned int x,i;

    while(1)
    {
        scanf("%d",&x);

        for(i=x-1;i>0;i--)
        {
           if(i%2==0)
           {
               printf("%d\n",i);
           }
        }

    }
    return 0;
}
