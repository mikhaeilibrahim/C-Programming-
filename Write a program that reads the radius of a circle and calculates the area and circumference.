int main()
{
    double a,c,r,by=22.0/7;
    while(1)
    {
        printf("Enter radius of circle\n");
        scanf("%lf",&r);

        a=r*r*by;
        c=2*by*r;

        printf("Area=%lf  Circumference=%lf\n",a,c);
    }

    return 0;
}
