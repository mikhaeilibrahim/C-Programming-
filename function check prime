int isprime(int n)
{
    int i;
    if(n<2)
    {
        return 0;  //not prime
    }

    for(i=2; i<n; i=i+1)
    {
        if(n%i==0)
        {
            return 0; //not prime
        }
    }

    return 1;   //prime
}
